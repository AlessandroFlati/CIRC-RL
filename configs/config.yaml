defaults:
  - causal_discovery: pc
  - policy: mlp
  - training: default
  - environments: cartpole
  - constraints: default
  - regularization: default
  - env_param_discovery: default
  - _self_

seed: 42
device: cuda
log_dir: outputs/${now:%Y-%m-%d_%H-%M-%S}
experiment_name: circ_rl_run
